[build]
  base = "."
  command = "npm install && npm run build -w web"
  publish = "web/.next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_PUBLIC_API_BASE_URL = "https://tk-tikicom-production.up.railway.app"
